// 8 variable types
// 7 numeric and boolean
// 7 numeric
    // 6 signed
        // uses numbers
        // byte, short, int, float, double
        // positive or negative
    // 1 unsigned
        // char
        // will only take positive numbers

// access modifers
    // private, default, protected, public
    // all classes fall in 2 categories
        // 1. top level class
        // public or default
        // non-top level class

// classes
// only one class can be a 'public class'
// what can be used
    // outside class structure
        // comments
        // declare package
            // can only declare up to one per file
            // acts as folder
        // import other files to be used in the class

    // inside class
        //
// widening context
    // replacing one primitive type with another primitive type for assignment-compatibility

// begin class with domain (com, net, edu)
    // followed by classname (nouns) usually capitalize first letter of each word


// data members 
    // static data, instance data, fields
    // methods  blocks (anonymous, or named)
        // anonymous blocks (auziliary, static and non-static)
        // named blocks  same types as anonymous

    // inner classes 
        // static and non-static

    // inner interfaces and or enums

    // can have a class inside another class
        // 